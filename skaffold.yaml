# Skaffold config for Cloud Deploy

apiVersion: skaffold/v4beta7
kind: Config
metadata:
  name: mlops-model-serving
build:
  artifacts:
    - image: serving-image
      docker:
        dockerfile: Dockerfile.serving
manifests:
  rawYaml:
    - k8s-manifests/*.yaml
deploy:
  kubectl: {}
profiles:
  - name: staging
    manifests:
      rawYaml:
        - k8s-manifests/*.yaml
    deploy:
      kubectl:
        defaultNamespace: staging
  - name: production
    manifests:
      rawYaml:
        - k8s-manifests/*.yaml
    deploy:
      kubectl:
        defaultNamespace: production
