# Pod Disruption Budget for Model Serving
# Ensures high availability during node maintenance or updates
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: model-serving-pdb
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: model-serving
